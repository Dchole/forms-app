overwrite: true
schema: "src/apollo/schema.gql"
documents: null
generates:
  src/apollo/types/generated.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-mongodb"
